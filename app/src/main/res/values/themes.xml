<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.UIBasics" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Disable splash icon on Android 12+ (safe to include even for older targets) -->
    </style>

    <style name="Theme.UIBasics" parent="Base.Theme.UIBasics" />

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item> <!-- Or your background color -->
        <item name="android:windowDisablePreview">true</item> <!-- Prevents the Android logo flash -->
    </style>

</resources>